Using the included views handlers . . .
You'll want to copy them to where you're keeping your module's views api stuff.
Once you've defined your field and set it to use the handler, then you want to
go into the handler code and make sure the $values->field is set right.